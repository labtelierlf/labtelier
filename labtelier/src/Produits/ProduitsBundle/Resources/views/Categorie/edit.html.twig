{% extends '::base.html.twig' %}
{% block body -%}
	<div class="container">
		<div class="row">
			<div id="entete" class="span12">
				<h2>Modifier Catégorie <i class="icon-edit"></i></h2>
				<div class="liens paddingRight span9 bords">
					{{ form(edit_form) }}
					<div class="wrapper">
						<div class="parent">
							<a href="{{ path('admin_categories') }}" class="bouton children">Retour liste</a>
						</div>
						<br/>
						{{ form(delete_form) }}
					</div>
				</div>
			</div>
		</div>
	</div>
{% endblock %}