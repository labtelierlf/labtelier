{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Poste</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Numeroposte</th>
                <td>{{ entity.numeroPoste }}</td>
            </tr>
            <tr>
                <th>Calendrier</th>
                <td>{{ entity.calendrier }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_postes') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('admin_postes_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
